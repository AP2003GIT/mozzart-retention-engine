spring:
  application:
    name: mozzart-retention-engine-backend

server:
  port: 8787

management:
  endpoints:
    web:
      exposure:
        include: health,info

retention:
  persistence: file
  data-file: ../.retention-spring-backend.json
  stream-interval-ms: 2200
